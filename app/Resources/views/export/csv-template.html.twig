{% autoescape false %}
Seller ID, Seller's Location, Seller's Rank, Member Since, Positive, Neutral, Negative, Items For Sale, Seller's Page, Seller's Store Page, Timestamp
{% for row in data %}
{{ row.sellerId }},{{ row.sellerLocation }},{{ row.sellerRank }},{{ row.memberSince }},{{ row.positive }},{{ row.neutral }},{{ row.negative }},{{ row.itemsForSale }},{{ row.sellerPage }}, {{ row.sellerStorePage }}, {{ row.timeStamp }}
{% endfor %}
{% endautoescape %}
